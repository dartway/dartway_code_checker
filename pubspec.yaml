name: dartway_code_checker
description: DartWay.dev code checker showing errors and warning from the Dart Way application design principles.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.3.0 <4.0.0"

executables:
  check_flutter_app: bin/check_flutter_app.dart
  calculate_flutter_app_stats: bin/calculate_flutter_app_stats.dart

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
